Good audit. Now execute the minimum truth pass.

We do not want a redesign and we do not want scope expansion.

We want the smallest set of changes that upgrades Hashapp from overclaiming polished prototype to honest prototype with one real connected permission path.

Mission
Based on your audit, make the minimum changes required to:

remove false or broken trust claims
remove or disable dead core interactions
connect one real “Grant Permission” flow to the existing Base-native proof path
surface one real proof link / tx hash
keep the overall product feel intact
Important context
The Base Sepolia spend-permission proof already exists from a different technical Replit agent as a standalone artifact.

Do not rebuild that proof from scratch.
Instead, use it as the technical primitive you are connecting the product to.

Your job is to close the gap between:
the current app shell
and
that already-proven primitive

Priority order
P0 — honesty fixes
First, remove or correct all false claims:
fake tx hashes
broken Basescan links
“proof available” where no proof exists
“rules enforced onchain via smart wallet” if not actually true
any other copy that implies live plumbing that does not exist

P0 — dead interaction cleanup
Remove, disable, or clearly demote dead CTAs that undermine trust:
Increase Scout’s limit
Adjust Scout’s budget
Pause Scout
non-functional arrows/cards
any fake-interactive element in the core trust path

Do not leave fake affordances in a trust product.

P1 — one real permission connection
Make Grant Permission the first real bridge into the proven Base-native path.
At minimum, I want:
real wallet connection or a clearly real demo-wallet path
a real permission transaction or a real invocation path tied to the existing proof implementation
a real returned tx hash
a real Basescan link
a real reflected state change in the UI

If full end-to-end integration is too much for one pass, then:
make the UI honest
connect the first real proof artifact cleanly
stop before faking anything else

P1 — minimal real state
Read back at least one real piece of state from the connected/proven flow:
wallet address
permission created state
tx hash / proof link
balance or allowance if feasible

P2 — persistence
If quick and low-risk, persist the relevant demo/app state across refresh so the experience doesn’t fully reset.

Constraints
no redesign
no major architecture rewrite
no scope widening
preserve the premium consumer feel
preserve the top-level structure: Money / Activity / Scout / Rules
optimize for truth, not breadth

Output I want back
Please return:

A. What you changed
Exact files, interactions, and copy updates.

B. What is now real
What is genuinely connected after this pass.

C. What is still simulated
What remains mocked or decorative.

D. Remaining blockers
What still prevents Hashapp from being called a working product.

E. Evidence
Any real tx hash, real proof link, wallet connection proof, or state read produced by this pass.

Standard
If something cannot be made real in this pass, make it honest.
Do not leave fake trust signals in place.
